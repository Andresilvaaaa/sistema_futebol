# Cache e compilados Python
__pycache__/
*.pyc

# Banco local
backend/instance/futebol_dev.db
# Ignorar quaisquer bancos locais na pasta instance (raiz e backend)
instance/*.db
backend/instance/*.db

# Ambiente virtual
venv/
# Arquivos de ambiente (evitar vazar segredos)
.env
backend/.env
frontend/.env

# Uploads locais (gerados em runtime)
uploads/
